# Reference harness router status

The common router contract is now represented by a dependency-free router, five optional adapter boundaries and offline contract tests.

The implementation deliberately does not claim a real ODS/Magnitude/Buddy/DeepSeek Harness execution. Real execution belongs to the empirical smoke/measurement gate.
