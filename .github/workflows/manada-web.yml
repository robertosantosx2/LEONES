name: Manada statistics
on:
  push:
    paths:
      - 'results/manada/**'
      - 'scripts/leones-manada-stats.py'
      - 'web/**'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install matplotlib
      - run: python3 scripts/leones-manada-stats.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add results/manada/stats
          git diff --cached --quiet || git commit -m 'chore: update Manada statistics'
          git push
